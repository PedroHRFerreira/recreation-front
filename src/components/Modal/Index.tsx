import style from "./styles.module.scss";

const Modal = () => {
  return (
    <aside className={style.backdrop}>
      <div className={style.modal}>nejndjnej</div>
    </aside>
  );
};

export default Modal;
